# v2 isolated APK build

Artifact: `.dist/coinpoker-hmuriy-body-disabled-debug.apk`

SHA-256: `73E2113CC28C282C506A2FF9CD43FCDFC7032F45B0550A23B0292A11A11AC7C3`

Size: 213,043,067 bytes.

`apksigner verify --verbose` passed with APK Signature Scheme v2 and v3. This proves packaging/signature integrity only; it does not prove runtime hook, discovery, hint, CC, or reconnect behavior.

The decompiled workspace and tooling are intentionally kept out of Git. A future rebuild should use a fresh isolated copy and record its source hash first.
